[pytest]
testpaths = tests
# Gateway/API tests hit the real local Postgres (see tests/conftest.py) rather
# than mocking the DB — this is a small project and the database IS the
# behavior worth testing (cache writes, cascades, llm_calls rows).
addopts = -ra
